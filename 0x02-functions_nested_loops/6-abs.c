#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: int that check
 * Return: the absolute value if integer
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
